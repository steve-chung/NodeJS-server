const Hole = require('../models/holes')


// exports.postHoles = (req, res, next) => {
//   const holes = req.body.holes
//   const newHoles
//   console.log(req.decode.user)
//   const user = req.decode.user
//   for (let key in holes) {
//     newHoles = Hole
//       .create( { hole_number: key, par: holes[key] }
//     )
//   }
// }